export const NORAD_IDS = Object.freeze({
  ISS: 25544,
  HUBBLE: 20580,
  CSS: 48274,
  BGS: 42801,
  ACS: 59588,
});

export const ISS = Object.freeze({
  ORBIT_POINTS: 380,
  PERIOD_BETWEEN_POINTS: 15,
});

export const HUBBLE = Object.freeze({
  ORBIT_POINTS: 400,
  PERIOD_BETWEEN_POINTS: 15,
});

export const CSS = Object.freeze({
  ORBIT_POINTS: 380,
  PERIOD_BETWEEN_POINTS: 15,
});

export const BGS = Object.freeze({
  ORBIT_POINTS: 70,
  PERIOD_BETWEEN_POINTS: 50,
});

export const ACS = Object.freeze({
  ORBIT_POINTS: 380,
  PERIOD_BETWEEN_POINTS: 15,
});
